# Run artifacts record site URLs, tenant IDs, folder paths, and account names,
# and both scripts default to writing them next to themselves, which is this repo.
Relabel-Files-*.log
Relabel-Files-*.csv
New-LabelTestSite-*.log
LabelTestSiteCleanup-*.json

# Development-only validation and repository automation are not part of the published utility.
/Tests/
/.github/

# Certificates, in case one is ever exported next to the scripts.
*.pfx
*.p12
*.cer
*.key
